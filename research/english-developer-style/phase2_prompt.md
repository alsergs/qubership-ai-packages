# Research title

Deep evaluation of proven editorial sources for natural, clear developer documentation generated by LLMs

# Goal

This is Pass 2 of the research.

Pass 1 found that there is probably no single proven off-the-shelf LLM skill for this exact problem. The strongest direction appears to be combining:

- authoritative software-documentation style guides
- prose linters and rule catalogues
- British-English plain-language guidance
- a small amount of LLM-prose-tell research
- narrow structural specs for commit messages and similar formats

Now deeply evaluate the shortlisted candidates below and recommend what should become the basis of a compact `SKILL.md`.

The target outcome is not “AI humanization” or AI-detector evasion. The target outcome is developer documentation that is clear, precise, maintainable, and natural enough that native English speakers do not reject it as awkward or obviously LLM-written.

# Target domain

Developer-facing English prose:

- technical documentation
- README files
- Javadoc / KDoc / docstrings / TSDoc / Go doc comments
- inline code comments
- changelog and release-note entries
- commit messages
- PR descriptions and review replies
- developer-facing error messages and log messages

# Primary preference

British English is the primary target.

However, strong US-English sources are allowed if they are more authoritative, better maintained, or more relevant to developer documentation.

The final recommendation may be:

- one British-English-first skill
- separate British and US variants
- one skill with a dialect switch
- a core skill plus dialect-specific appendix

Prefer developer-documentation clarity over generic “native-like” prose.

# Important exclusions

Do not recommend:

- AI-humanizer services
- detector-evasion prompts
- paraphrasers aimed at bypassing AI detectors
- marketing-copy prompts
- fiction-writing prompts
- viral social-media writing prompts
- grammar-only tools with no style or technical-writing guidance
- anonymous prompt snippets with no evidence of editorial quality

Do not recommend rules whose primary effect is to evade AI detectors rather than improve reader experience.

# Shortlisted candidates from Pass 1

Deeply evaluate these candidates:

1. Google Developer Documentation Style Guide  
   URL: https://developers.google.com/style  
   Why shortlisted: de facto baseline for developer-doc voice, word choice, code-in-text rules, and authoring/editing guidance.

2. Microsoft Writing Style Guide  
   URL: https://learn.microsoft.com/en-us/style-guide/welcome/  
   Why shortlisted: strong “simple and human” voice, contractions guidance, sentence case, accessibility, global English.

3. GitLab Documentation Style Guide and Vale rules  
   URL: https://docs.gitlab.com/development/documentation/styleguide/  
   Why shortlisted: real-world docs style guide plus concrete Vale rules; especially promising for linting/review and portable rules.

4. Vale and errata-ai style packages  
   URL: https://vale.sh/  
   Why shortlisted: rule catalogues are close to machine-checkable editorial guidance; includes ports of Google, Microsoft, IBM, write-good, proselint, alex, readability, and similar packages.

5. Wikipedia: Signs of AI writing  
   URL: https://en.wikipedia.org/wiki/Wikipedia:Signs_of_AI_writing  
   Why shortlisted: community-curated catalogue of LLM prose tells; useful only as background for failure modes, not as detector-evasion advice.

6. proselint  
   URL: https://github.com/amperser/proselint  
   Why shortlisted: concrete editorial lint rules derived from named prose authorities; likely useful for clichés, weasel words, uncomparables, and overstatement.

7. GOV.UK Style Guide / GOV.UK content guidance  
   URL: https://www.gov.uk/guidance/style-guide  
   Why shortlisted: strongest British-English plain-language authority; useful for British default, plain English, and words-to-avoid.

8. Kubernetes Documentation Style Guide  
   URL: https://kubernetes.io/docs/contribute/style/style-guide/  
   Why shortlisted: large OSS developer-doc style guide; practical extension of Google-style rules for API objects, placeholders, commands, and docs conventions.

9. Atlassian Design System content guidance  
   URL: https://atlassian.design/content  
   Why shortlisted: useful for developer-facing UI copy, error messages, log messages, and tone in failure states.

10. Conventional Commits  
   URL: https://www.conventionalcommits.org/en/v1.0.0/  
   Why shortlisted: canonical structural guidance for commit messages; useful as a small scoped module, not as a general prose guide.

# Specific questions from Pass 1 to investigate

Investigate these uncertainties:

1. Does each source provide worked examples or before/after examples?
2. Are there false-positive complaints or open issues showing that rules can produce awkward prose?
3. Does the source support British English, US English, both, or neither?
4. Can the source be compressed into a practical `SKILL.md` without importing hundreds of rules?
5. Does the source contradict the baseline skill’s stricter rules, especially:
   - strict em-dash avoidance
   - no em-dashes in headings
   - no em-dashes in definition-list style bullets
   - splitting long mechanically balanced sentences
   - avoiding stacked hedges
   - avoiding page-topic self-description
   - gerund after “rather than” / “instead of”
6. Which rules should be implemented as LLM guidance, and which should be left to Vale/textlint?
7. Which rules are likely to be harmful if applied mechanically?
8. Is there a better source for error-message and log-message prose than Atlassian/Microsoft/Apple/GitLab?
9. Is there a richer British-English Vale/style-rule source than GitLab’s British-English detector?
10. Should the final result be:
    - one editing-oriented skill
    - separate authoring and editing skills
    - one skill plus a Vale/textlint config
    - a core skill plus domain modules for docs/comments/commits/errors

# Required output

Produce a research report under 3500 words.

Use citations and source URLs. Prefer official documentation, repo issue trackers, and primary sources.

Do not spend space re-explaining generic technical writing principles unless they affect the final skill design.

## 1. Executive summary

Answer directly:

- What are the top 5 sources after deep evaluation?
- Is there a single best base for the skill?
- What should be the primary base: Google, Microsoft, GitLab, GOV.UK, Vale, or a synthesis?
- Should the final skill be British-first, dialect-switching, or split into British/US variants?
- Should it be authoring-oriented, editing-oriented, or split?
- What should be handled by the LLM skill vs by Vale/textlint?

## 2. Deep candidate evaluation

For each of the 10 shortlisted candidates, provide a compact evaluation table with:

- name
- source URL
- final rank or disposition:
  - top-5 source
  - supporting source
  - background only
  - reject for this use case
- author / organization
- dialect applicability
- best use:
  - authoring
  - editing / rewrite
  - linting / review
  - background research
  - narrow module
- strongest contribution
- main weakness
- evidence of authority or adoption
- maintenance status
- examples available: yes / no / partial
- false-positive or awkward-output risk: low / medium / high
- portability to compact `SKILL.md`: easy / medium / hard

## 3. What each top source contributes to a better skill

For each top-5 source, explain:

- which specific rule families it should contribute
- what it covers that the baseline skill misses
- what it says differently from the baseline
- whether any contradiction with the baseline is useful
- whether it is better as authoring guidance, editing guidance, or linting guidance

Focus on rule families, not exhaustive rule lists.

## 4. Worked examples

For each top-5 source:

- find up to two short worked examples from the source’s own docs, tests, or rule documentation
- if no examples exist, say so
- explain what the example teaches for the future skill

Keep examples short. Do not quote long passages.

## 5. Pitfalls and false positives

For each top-5 source, check:

- repo issues
- docs caveats
- community complaints
- known conflicts with other style guides
- cases where rules produce awkward/stilted prose
- cases where rules are too US-centric or incompatible with British English
- cases where rules are too generic for developer docs

Pay special attention to known criticism of:

- passive-voice linters
- readability-score rules
- weasel-word detectors
- inclusive-language linters
- strict punctuation rules
- blanket “avoid X” rules

## 6. British-English strategy

Recommend how to support British English.

Answer:

- Which source should define British spelling and punctuation defaults?
- Should GOV.UK be the primary British source?
- Which US developer-doc rules can safely be reused under British spelling?
- Which rules need dialect-specific variants?
- Is there a usable British-English Vale/textlint package?
- How should the skill handle repositories that already use US English?

## 7. Authoring vs editing strategy

Recommend whether the final deliverable should be:

- one editing-oriented skill that can also guide authoring
- separate authoring and editing skills
- one skill with sections for authoring, editing, linting, and final pass

Explain the trade-off.

My current preference is an editing/rewrite-oriented skill, because the immediate problem is LLM drafts that sound unnatural. However, include authoring guidance if it prevents the bad prose from appearing in the first place.

## 8. LLM skill vs linter split

Recommend which rule families belong in:

1. `SKILL.md` guidance for the LLM
2. Vale/textlint/proselint-style automated checks
3. optional human-review checklist

Examples:

- dialect consistency
- em-dash overuse
- weak hedging
- long sentence splitting
- passive voice
- inclusive language
- terminology word lists
- code-in-text formatting
- commit-message structure
- error-message structure
- AI-tell avoidance

## 9. Recommended synthesis

Propose the final architecture.

Include:

- primary base source
- secondary sources
- background-only sources
- sources to avoid
- proposed skill shape:
  - sections
  - approximate length
  - whether examples are needed
  - whether to include positive examples, negative examples, or both
- whether to create companion Vale/textlint config

End with 10–15 concrete rule families that should be ported into the final software-prose skill.

# Baseline skill

Use the following skill only as context. Do not treat it as the target.

---
---
name: english-prose-style
description: English prose conventions for written deliverables: documentation, README files, javadoc and code comments, commit messages, PR descriptions and review replies, design docs, and choice of English identifier names (methods, variables, classes, files). Covers em-dash usage, hyphen stacking, sentence structure, that/which, gerunds after "rather than", hedging, inclusive language, and British/American dialect consistency. Use whenever the deliverable is English prose or an English identifier that will be read by humans. Do not use for casual chat replies to the user.
---
# English prose style
Conventions for written English that will be read by humans: docs,
javadoc, commit messages, PR descriptions, identifier names. These rules
target the mechanical drift patterns LLMs fall into when writing
technical prose. They are deliberately scoped to genuine LLM failure
modes; basic grammar (subject-verb agreement, possessives,
capitalisation, punctuation spacing) is assumed and not repeated here.
## When to apply
Apply these rules when producing:
- Documentation (Markdown, Asciidoc, plain text)
- README files, CHANGELOG entries, release notes
- Javadoc, KDoc, docstrings, inline code comments
- Commit messages, PR titles, PR descriptions, review replies
- Design docs, RFCs, architecture notes
- English identifier names (method, variable, class, file, package)
- Error messages and user-facing log strings
## Dialect
Default to **British English** unless the project specifies otherwise
(check the project CLAUDE.md or repository conventions). Common project
signals: existing prose using `behaviour` / `organisation` / `colour`
(British) vs `behavior` / `organization` / `color` (American). Match the
existing dialect rather than overriding it.
LLMs tend to drift towards American spelling in long technical documents
because the American corpus dominates training data. On edits longer than
a few paragraphs, re-read the output and check for consistency before
finishing.
## Em-dash usage
Em-dash (`—`) is the single most-flagged issue in LLM-generated technical
prose. The rules below are strict.
- **No em-dash in section headings.** Use a colon: `## reWriteBatchedInserts=true: batch INSERT throughput`. If the qualifier does not fit a colon cleanly, move it into the intro sentence below the heading.
- **No em-dash in definition lists.** Patterns like `**Term** — definition` (status labels, glossary entries, bullet annotations) should be `**Term**: definition`.
- **No paired em-dashes for parentheticals.** `X — qualifier — Y` becomes `X (qualifier), Y`, `X, qualifier, Y`, or two separate sentences.
- **No em-dash as a paragraph-level connector.** `We value backward compatibility — upgrading is...` should be a period (`We value backward compatibility. Upgrading is...`), a semicolon, or a colon depending on the relationship between the two clauses.
- **No em-dash to introduce a list.** Use a colon: `bounded by its own timeout: TCP handshake, optional TLS upgrade, ...` (not `... by its own timeout — TCP handshake, ...`).
- **Use commas for short asides.** "The driver, by default, opens one socket" needs no dash.
- **Reserve the em-dash for genuine sharp emphasis** where comma, colon, semicolon, period, and parentheses would all lose the break. Even then, aim for at most one per page.
If a paragraph contains an em-dash, default to rewriting it. Most uses
survive a comma, colon, semicolon, period, or parentheses without losing
meaning.
## Prose patterns
LLM drafts fall into a small set of structural patterns that
native-English reviewers flag immediately. Light conversational phrasing
("the right default", "the knob you actually want") and standard
component-as-agent voice ("the driver bundles", "the parser accepts") are
fine. The goal is to head off the mechanical issues.
### Limit hyphen stacking in noun phrases
When several hyphenated modifiers stack inside a single noun phrase, the
reader has to mentally unwind each one before reading on. Two hyphens in
a row is usually the comfortable limit.
- Hard to parse: `the per-batch socket-buffer deadlock`
- Easier: `the deadlock that happens when the per-batch buffer fills`
- Hard to parse: `the out-of-band cancel side-channel TCP connection`
- Easier: `the second TCP connection that carries the cancel signal`
If a noun phrase has three or more compound modifiers in a row, rewrite
it as a clause.
### Split sentences earlier than you think
Aim for one main idea per sentence. If a sentence joins two distinct
claims with a semicolon or a comma-plus-conjunction, or carries more than
one subordinate clause, consider splitting. The fact that an existing
paragraph already has long sentences is not evidence that the style fits.
- Heavy: `The driver pipelines the writes, so the wall-clock cost is not literally N × round-trip; the cost is still high, though, because before server-prepare warms up there is per-execution parse work, and even after that the response stream still carries one CommandComplete per row.`
- Split: `The driver pipelines the writes, so the wall-clock cost is not literally N × round-trip. The cost is still high. Before server-prepare warms up, every execution carries parse work; after that, the response stream still returns one CommandComplete per row.`
### Relative clauses: `that` / `which` / `what`
Use `that` for restrictive (defining) clauses; reserve `which` for
non-restrictive (parenthetical) clauses, set off by commas:
- Restrictive: `the timeout that bounds the TCP handshake`
- Non-restrictive: `the timeout, which is shared across hosts, bounds the TCP handshake`
When the relative clause modifies a specific noun, do not fuse it with
`what` ("the thing that"). Fused-relative `what` is grammatical but reads
as conversational in technical prose:
- Less natural: `Each comes with one specific caveat (what is holding it back from being on by default)...`
- More natural: `Each comes with one specific caveat (the reason it is off by default)...`
`What` is fine in non-relative use (`do what the property says`).
### Use the gerund after `rather than` / `instead of`
When the comparison is between two actions, both sides need the gerund
form. Truncating to the bare verb reads as colloquial shorthand.
- Clipped: `rather than copy-paste`, `instead of send a separate SET`
- Full: `rather than copy-pasting`, `instead of sending a separate SET`
### Hedge once, not three times
A single hedge (`in practice`, `typically`, `in most deployments`,
`usually`) does the work. When several stack in one sentence, keep the
strongest and drop the rest.
- Bloated: `In practice, on most deployments, the savings are typically modest in the same-DC case.`
- Tight: `On a same-DC network the savings are modest.`
### Don't define the page topic in its own body
A paragraph that explains what its containing page is about ("This page
is the explanation that backs the X entry...") rarely adds information;
the heading and the front-matter `description` already carry that signal.
Lead with the substance.
### Watch for missing or duplicated small words
LLM auto-completion occasionally drops or duplicates short function words
(`is`, `are`, `have`, `will be`, `you can`, `we`). Re-read for them:
- Missing verb: `There a number of restrictions` → `There are a number of restrictions`
- Extra word: `will have be in UTC` → `will be in UTC`
- Extra word: `you can use issue a query` → `you can issue a query`
## Inclusive language
Replace exclusionary terms with neutral alternatives:
- `master switch` / `master config` → `top-level switch` / `main config`
- `blacklist` / `whitelist` → `blocklist` / `allowlist`
- Replication topology: `primary` / `standby` / `replica` rather than `master` / `slave`
- `sanity check` → `quick check` / `consistency check`
- `dummy value` → `placeholder value`
When the term is a literal identifier (config value, class name, CLI
flag, external API), keep the literal token unchanged.
## Identifier names
The dialect, inclusive-language, and brand-spelling rules above apply to
identifier names too. Match the surrounding code: if a file already uses
`parseURL`, do not introduce `parseUrl`; if a project is British, write
`initialiseConnection`. Verify brand spelling (`PostgresConnection`, not
`PostresConnection`).
## Final pass before finishing
For non-trivial English deliverables, re-read looking specifically for
the LLM-typical failure modes:
1. Any em-dash in the output: almost always rewrite.
2. Dialect drift: pick one variant and normalise.
3. Hyphen stacks of three or more modifiers in one noun phrase.
4. Stacked hedges in one sentence.
5. Dropped or duplicated small function words.
6. Brand spelling and acronym casing (`PostgreSQL`, `GitHub`, `URL`, `JDBC`, `TLS`).
